; デフォルトでは npmjs のパブリックレジストリを参照します。
; 社内ミラーを利用する場合は、このファイルを適切な URL と認証設定に更新してください。
registry=https://registry.npmjs.org/

; プライベートレジストリを使う場合は、以下の例を参考にトークンと always-auth を設定してください。
; //registry.example.com/:_authToken=${NPM_TOKEN}
; always-auth=true
